const db = require('../db/mongodbCon.js');

function bookmark () {

const bookmark = "INSERT INTO USER.bookmarkList VALUES (Project.id);";

    return bookmark
}

function bookmarkList() { 

    const bookmarkList = "select user.bookmarkList asc "

    return bookmarkList

}